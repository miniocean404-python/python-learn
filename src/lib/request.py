import requests
import qrcode


def request_baidu(url: str = "https://www.baidu.com") -> None:
    # r = requests.get(url)
    # print(r.status_code)
    # qrcode.make(str(r.status_code))
    # img.save("baidu.png")
    print(1234567)
    return
